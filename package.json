{
  "name": "kelompok7-root",
  "version": "1.0.0",
  "description": "Root package runner for Kelompok 7 vehicle database system",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "compile-c": "gcc -O2 backend-c/kendaraan.c -o backend-c/kendaraan.exe"
  }
}
